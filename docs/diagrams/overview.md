# 🎬 UML Overview — Movie Collection App

**Лабораторные работы №3–4**  
по дисциплине *«Объектно-ориентированное проектирование систем. Язык UML»*

---

## 📖 Описание системы

**Название системы:** Movie Collection App  
**Тип:** веб/мобильное приложение  

**Назначение системы:**  
Позволяет пользователю вести собственную коллекцию фильмов — добавлять, сортировать, просматривать, оценивать, получать рекомендации и искать новые фильмы через внешний API.  
Приложение ориентировано на персонализацию и удобный доступ к информации о фильмах.

---

## 📘 Глоссарий и сценарии

| Файл | Назначение |
|------|-------------|
| [`glossary.md`](glossary.md) | Глоссарий ключевых терминов: User, Movie, Collection, Recommendation, Tag, Rating и др. |
| [`flows/add_movie.md`](flows/add_movie.md) | Сценарий добавления фильма в коллекцию, включая ошибки и GUI-действия. |
| [`flows/get_recs.md`](flows/get_recs.md) | Сценарий получения рекомендаций для пользователя. |
| [`flows/view_movie.md`](flows/view_movie.md) | Сценарий просмотра карточки фильма и работы с ней. |

---

## ⚙️ UML Диаграммы

Ниже приведены все разработанные диаграммы с подробными пояснениями.

---

### 1️⃣ **Диаграмма вариантов использования (Use Case Diagram)**  

![](images/usecase_main.png)

**Описание:**  
Диаграмма демонстрирует, какие функции предоставляет система и какие актёры с ней взаимодействуют.  
- **Guest (гость)** — неавторизованный пользователь, может просматривать публичную информацию, регистрироваться и искать фильмы.  
- **User (пользователь)** — авторизованный пользователь, управляющий коллекцией.  

**Основные сценарии использования:**  
- Регистрация и вход в систему (`Sign Up / Sign In`)  
- Поиск фильмов (`Search Movies`)  
- Просмотр карточки фильма (`View Movie Details`)  
- Добавление/удаление фильма в коллекцию (`Add / Remove Movie`)  
- Оценка и тегирование (`Rate or Tag Movie`)  
- Получение рекомендаций (`Get Recommendations`)  
- Просмотр коллекции (`View My Collection`)  
- Экспорт/импорт коллекции (`Export / Import`)  

**Связи:**  
- `Add Movie` включает `Search Movies` (include)  
- `View Movie Details` расширяет `Add Movie` и `Rate Movie` (extend)  
- `Get Recommendations` включает `View Movie Details`  

Эта диаграмма отражает полный перечень пользовательских возможностей системы.

---

### 2️⃣ **Диаграммы активностей (Activity Diagrams)**  

#### 🟢 Получение рекомендаций  
![](images/activity_get_recs.png)

**Описание:**  
Пользователь открывает вкладку «Рекомендации».  
Система анализирует его профиль предпочтений (жанры, оценки, теги), обращается к сервису рекомендаций и формирует список фильмов.  
В диаграмме показаны альтернативные ветки — отсутствие данных для анализа, ошибка сети и сбой Recommendation Service.  

**Ключевые состояния:**  
1. Анализ предпочтений  
2. Генерация рекомендаций  
3. Отображение карточек фильмов  
4. Возможность повторного запроса или оценивания  

---

#### 🟢 Добавление фильма в коллекцию  
![](images/activity_add_movie.png)

**Описание:**  
Диаграмма описывает процесс поиска и добавления фильма в коллекцию пользователя.  
Показано, как система проверяет наличие фильма, выполняет запрос к внешнему API и сохраняет результат.  

**Основные ветви:**  
- Успешное добавление фильма;  
- Фильм уже существует в коллекции;  
- Ошибка сети или пустой результат поиска.  

**GUI-события:** поиск, выбор фильма, подтверждение добавления, отображение уведомлений.

---

#### 🟢 Просмотр карточки фильма  
![](images/activity_view_movie.png)

**Описание:**  
Пользователь выбирает фильм из поиска, рекомендаций или коллекции.  
Система получает данные о фильме из API, отображает карточку с подробным описанием, позволяет добавить, оценить или отметить фильм как просмотренный.  

**Ветвления:**  
- Успешная загрузка данных;  
- Ошибка API;  
- Неавторизованный пользователь;  
- Повторная попытка загрузки.  

Карточка фильма — центральный элемент приложения, связывающий все функции.

---

### 3️⃣ **Диаграммы последовательностей (Sequence Diagrams)**  

#### 🔵 Добавление фильма в коллекцию  
![](images/sequence_add_movie.png)

**Описание:**  
Показано взаимодействие между пользователем, интерфейсом (UI), контроллером, репозиторием, базой данных и внешним API.  
- UI отправляет запрос `searchMovie()`, получает список фильмов;  
- Пользователь выбирает фильм, система запрашивает детали;  
- При нажатии «Добавить» происходит проверка наличия и добавление записи в базу.  

Диаграмма демонстрирует последовательность вызовов и обмен сообщениями.

---

#### 🔵 Получение рекомендаций  
![](images/sequence_get_recs.png)

**Описание:**  
UI вызывает контроллер рекомендаций, который анализирует предпочтения пользователя и запрашивает данные из базы и внешнего API.  
Показано, как результат передаётся обратно пользователю, а также как обновляется профиль предпочтений.  
Подчёркнута цикличность взаимодействия — после оценки фильмов система улучшает рекомендации.

---

#### 🔵 Просмотр карточки фильма  
![](images/sequence_view_movie.png)

**Описание:**  
Пользователь выбирает фильм, система сначала проверяет локальный кэш, затем обращается к внешнему API.  
Далее отображает карточку, позволяет добавить фильм в коллекцию, поставить оценку или отметить как просмотренный.  
Отражены проверки авторизации и сохранение изменений в базе.

---

### 4️⃣ **Диаграмма состояний (State Diagram)**  

![](images/state_movie_card.png)

**Описание:**  
Показывает жизненный цикл карточки фильма:  
1. **Не загружена** — пользователь только выбрал фильм.  
2. **Загружается** — отправлен запрос к API.  
3. **Загружена** — данные получены и показаны.  
4. **Добавлена в коллекцию** — пользователь добавил фильм.  
5. **Просмотрена** — пользователь отметил как просмотренный.  
6. **Оценена / Обновлена** — добавлена оценка или тег.  
7. **Удалена** — фильм исключён из коллекции.  

Диаграмма иллюстрирует динамику состояний объекта и переходы между ними при действиях пользователя.

---

### 5️⃣ **Диаграмма классов (Class Diagram)**  

![](images/class_domain.png)

**Описание:**  
Показывает структуру предметной области приложения и связи между сущностями.  
- **User** — хранит информацию о пользователе и его предпочтениях.  
- **Collection / CollectionItem** — представляет коллекцию фильмов и отдельные записи в ней.  
- **Movie / MovieDetails** — данные о фильме и подробная информация из внешнего API.  
- **RecommendationService, SearchService, AuthService** — служебные классы для логики приложения.  
- **MovieRepository** — взаимодействует с базой данных.  

Показаны все ключевые атрибуты, методы и ассоциации между объектами.

---

### 6️⃣ **Диаграмма компонентов (Component Diagram)**  

![](images/component_arch.png)

**Описание:**  
Показывает архитектурное разбиение системы на логические модули:

- **Client Side** — Web UI и Mobile App, предоставляют интерфейс пользователю.  
- **Backend API** — серверная часть с контроллерами и сервисами.  
  - `Auth Service` — аутентификация пользователей.  
  - `Movie Controller` — управление фильмами и коллекцией.  
  - `Recommendation Service` — генерация рекомендаций.  
  - `Search Service` — поиск фильмов.  
- **Data Sources** — база данных и внешний API фильмов (TMDb/OMDb).  

Диаграмма демонстрирует, какие компоненты связаны между собой, какие данные они обменивают и через какие интерфейсы (REST API, SQL).

---

### 7️⃣ **Диаграмма развёртывания (Deployment Diagram)**  

![](images/deployment_arch.png)

**Описание:**  
Показывает физическое размещение компонентов системы и каналы связи между ними.

- **User Device** — мобильное устройство или ПК с браузером, на котором работает пользователь.  
- **Application Server** — сервер, где запущены все контроллеры и сервисы Backend API.  
- **Database Server** — сервер или контейнер с базой данных (PostgreSQL/SQLite).  
- **External API** — внешний источник информации о фильмах (например, TMDb).  

Связи между компонентами показаны через протоколы:  
- HTTPS — для передачи данных между клиентом и сервером;  
- SQL — для взаимодействия Backend API с базой данных;  
- Внешние HTTPS-запросы — для получения данных о фильмах.  

Эта диаграмма описывает, как логические модули системы (из Component Diagram) развёрнуты на реальных физических или виртуальных узлах.

